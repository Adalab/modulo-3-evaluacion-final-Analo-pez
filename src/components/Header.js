import React from 'react';
import '../stylesheets/Header.scss';

function Header() {
    return (
        <div className="DivHeader">Preparando una buena estructura de REACT</div>
    );
}

export default Header;